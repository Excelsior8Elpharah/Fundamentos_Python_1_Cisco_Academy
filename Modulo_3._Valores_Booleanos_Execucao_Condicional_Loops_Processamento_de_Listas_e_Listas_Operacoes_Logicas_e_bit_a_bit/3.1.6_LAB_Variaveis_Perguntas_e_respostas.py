# Cenário:
# Usando um dos operadores de comparação em Python, escreva um programa simples de duas linhas que aceita o parâmetro n como entrada, que é um inteiro, e imprime False se n for menor que 100, e True se n for maior ou igual a 100.
# Não crie blocos if (falaremos sobre eles em breve). Teste seu código usando os dados que fornecemos para você.

# Solicitando ao usuário que insira um número
n = int(input("Digite um número: "))

# Imprimindo True se n for maior ou igual a 100, caso contrário imprime False
print(n >= 100)
